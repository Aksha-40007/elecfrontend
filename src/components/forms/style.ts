import {styled} from "styled-components";


export const StyledBackgroundForm = styled.div`
    background-color: white;
    box-shadow: 0px 8px 32px 0 rgba(0, 0, 255, 0.404);
    backdrop-filter: blur(3px);
    border-radius: 18px;
    margin:6rem 2rem;
    padding:1.5rem 2rem;
    position:absolute;
    .login-form {
        max-width: 100%;
      }
      .login-form-forgot {
        float: right;
      }
      .login-form-button {
        width: 100%;
      }
`;

